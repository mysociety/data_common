jobs:
  build:
    steps:
      - run: |
          dataset version auto --all --publish
      - name: Build with Jekyll
        uses: actions/jekyll-build-pages@example
        with:
          source: docs
          destination: docs/_site
      - name: Upload artifact
        with:
          path: docs/_site
